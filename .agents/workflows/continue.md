---
description: Continue the development cycle (commit, push, merge) automatically
---
// turbo-all
1. Check current git status and branch
2. Add all changes to git
3. Generate a suitable commit message and commit
4. Push to remote origin
5. Merge into develop if not already on it
6. Report completion and next steps
